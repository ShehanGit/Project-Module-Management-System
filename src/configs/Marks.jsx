								// <input 
								// 	type="text" 
								// 	className={`form-control ${(errors.xxxx ? 'is-invalid' : '') || (success.xxxx ? 'is-valid' : '')}`}
								// 	name="xxxx" 
								// 	placeholder="Disanayake D.M.S."
								// 	value={formData.xxxx} 
								// 	onChange={handleChange} 
								// />
								// {errors.xxxx && <div className="invalid-feedback">{errors.xxxx}</div>}
          						// {success.xxxx && <div className="valid-feedback">{success.xxxx}</div>}



								// <select 
								// 	name="xxxx" 
								// 	className={`form-control ${(errors.xxxx ? 'is-invalid' : '') || (success.xxxx ? 'is-valid' : '')}`}
								// 	value={formData.xxxx} 
								// 	onChange={handleChange}
								// >
								//   	<option value="">Select xxxx</option>
								// 	<option value="USA">USA</option>
								//   	<option value="Canada">Canada</option>
								//   	<option value="UK">UK</option>
								// </select>
								// {errors.xxxx && <div className="invalid-feedback">{errors.xxxx}</div>}
								// {success.xxxx && <div className="valid-feedback">{success.xxxx}</div>}


							// 	<div className="col-md-10 mb-1">
                            //     <label htmlFor="validationServer01">Member xxx name</label>
                            //     <input 
							// 		type="text" 
							// 		className={`form-control ${(errors.xxx_name ? 'is-invalid' : '') || (success.xxx_name ? 'is-valid' : '')}`}
							// 		name="xxx_name" 
							// 		placeholder="Disanayake D.M.S."
							// 		value={formData.xxx_name} 
							// 		onChange={handleChange} 
							// 	/>
							// 	{errors.xxx_name && <div className="invalid-feedback">{errors.xxx_name}</div>}
          					// 	{success.xxx_name && <div className="valid-feedback">{success.xxx_name}</div>}
                            // </div>
							// <div className="col-md-10 mb-1">
                            //     <label htmlFor="validationServer02">Student registration number</label>
							// 	<input 
							// 		type="text" 
							// 		className={`form-control ${(errors.xxxStudentID ? 'is-invalid' : '') || (success.xxxStudentID ? 'is-valid' : '')}`}
							// 		name="xxxStudentID" 
							// 		placeholder="Disanayake D.M.S."
							// 		value={formData.xxxStudentID} 
							// 		onChange={handleChange} 
							// 	/>
							// 	{errors.xxxStudentID && <div className="invalid-feedback">{errors.xxxStudentID}</div>}
          					// 	{success.xxxStudentID && <div className="valid-feedback">{success.xxxStudentID}</div>}
                            // </div>
							// <div class="col-md-10 mb-1">
							// 	<label for="validationDefault03">Contact number</label>
							// 	<input 
							// 		type="text" 
							// 		className={`form-control ${(errors.xxx_contact_number ? 'is-invalid' : '') || (success.xxx_contact_number ? 'is-valid' : '')}`}
							// 		name="xxx_contact_number" 
							// 		placeholder="0775434567"
							// 		value={formData.xxx_contact_number} 
							// 		onChange={handleChange} 
							// 	/>
							// 	{errors.xxx_contact_number && <div className="invalid-feedback">{errors.xxx_contact_number}</div>}
          					// 	{success.xxx_contact_number && <div className="valid-feedback">{success.xxx_contact_number}</div>}
							// </div>
                            // <div className="col-md-10 mb-1">
							// 	<label for="validationServer04">Email</label>
							// 	<input 
							// 		type="text" 
							// 		className={`form-control ${(errors.xxx_email ? 'is-invalid' : '') || (success.xxx_email ? 'is-valid' : '')}`}
							// 		name="xxx_email" 
							// 		placeholder="Disanayake D.M.S."
							// 		value={formData.xxx_email} 
							// 		onChange={handleChange} 
							// 	/>
							// 	{errors.xxx_email && <div className="invalid-feedback">{errors.xxx_email}</div>}
          					// 	{success.xxx_email && <div className="valid-feedback">{success.xxx_email}</div>}
							// </div>
							// <div className="col-md-10 mb-1">
							// 	<label for="validationServer05">Batch</label>
							// 	<select 
							// 		name="xxx_batch" 
							// 		className={`form-control ${(errors.xxx_batch ? 'is-invalid' : '') || (success.xxx_batch ? 'is-valid' : '')}`}
							// 		value={formData.xxx_batch} 
							// 		onChange={handleChange}
							// 	>
							// 	  	<option value="">Select batch</option>
							// 		<option value="USA">USA</option>
							// 	  	<option value="Canada">Canada</option>
							// 	  	<option value="UK">UK</option>
							// 	</select>
							// 	{errors.xxx_batch && <div className="invalid-feedback">{errors.xxx_batch}</div>}
							// 	{success.xxx_batch && <div className="valid-feedback">{success.xxx_batch}</div>}
							// </div>
							// <div class="col-md-10 mb-1">
							// 	<label for="validationDefault06">Specialization</label>
							// 	<select 
							// 		name="xxx_specialization" 
							// 		className={`form-control ${(errors.xxx_specialization ? 'is-invalid' : '') || (success.xxx_specialization ? 'is-valid' : '')}`}
							// 		value={formData.xxx_specialization} 
							// 		onChange={handleChange}
							// 	>
							// 	  	<option value="">Select specialization</option>
							// 		<option value="USA">USA</option>
							// 	  	<option value="Canada">Canada</option>
							// 	  	<option value="UK">UK</option>
							// 	</select>
							// 	{errors.xxx_specialization && <div className="invalid-feedback">{errors.xxx_specialization}</div>}
							// 	{success.xxx_specialization && <div className="valid-feedback">{success.xxx_specialization}</div>}
							// </div>